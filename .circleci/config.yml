version: 2
jobs:
  build:
    working_directory: ~/to-do-list
    docker:
      - image: circleci/node:latest-browsers
    steps:
      - checkout
      - run:
          name: update-npm
          command: 'yarn global add npm@latest'
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}
      - run:
          name: install-npm-wee
          command: yarn
      - save_cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - ./node_modules
      - run:
          name: test
          command: npm test
          background: true

      - run:
          name: Run the application
          command: yarn start
          background: true

      - run: yarn run selenium-setup
      - run: yarn run selenium-setup
      - run: yarn run e2e-tests

      - run: bash .circleci/setup-heroku.sh
      - add_ssh_keys:
          fingerprints:
            - "cf:7b:81:1d:0a:00:fd:c4:ab:ab:c5:cc:57:c0:89:0f"
      - deploy:
          name: Deploy Master to Heroku
          command: |
            if [ "${CIRCLE_BRANCH}" == "master" ]; then
              docker login --email=$HEROKU_LOGIN --username=$HEROKU_LOGIN --password=$HEROKU_API_KEY registry.heroku.com
              docker build --rm=false -t registry.heroku.com/salty-river-12539/web .
              docker push registry.heroku.com/salty-river-12539/web:latest
            fi
